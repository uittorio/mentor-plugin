[package]
name = "migrations"
edition = "2024"
version.workspace = true

[dependencies]
learning = { path = "../learning" }
libsql = {version = "0.9.30"}
tokio = {version = "1", features = ["full"]}
eyre = {version = "0.6.12"}
